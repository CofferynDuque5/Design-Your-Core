interface ImportMetaEnv {
  /** URL de la app web (botones de entrar y crear cuenta). */
  readonly PUBLIC_APP_URL?: string;
  /** Correo de contacto para privacidad y soporte (opcional). */
  readonly PUBLIC_CONTACT_EMAIL?: string;
}
